micronaut:
  application:
    name: userService
datasources:
  default:
    url: jdbc:mysql://localhost:3306/user-db
    username: root
    password: Manasgupta@1
    schema-generate: CREATE_DROP
    dialect: MYSQL
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true
netty:
  default:
    allocator:
      max-order: 3
